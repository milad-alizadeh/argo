import { ipcRenderer } from 'electron'
import { mainValue } from '@/domains/example/main/store'
import { providerValue } from '@/providers/endpoints'

export { ipcRenderer, mainValue, providerValue }
