# Visual direction comparison

This is an isolated experiment, not a change to the installed visual-exploration skill. Work only in your task's output directory. Do not read previous test outputs, other conversations, sibling experimental skills, or the installed visual-exploration instructions. Use the selected experimental skill as your design guidance; tools and their required operational instructions remain available.

Interview the user before designing. Establish the audience, main work, platform, desired feeling, existing identity, and constraints in one compact round. Wait for answers. Then produce four genuinely different visual directions for that brief, without another approval round before the first visual set.

For each direction, deliver two focused HTML studies: a substantial product or website composition with realistic content and at least two meaningful UI states or contexts, and a companion mood study showing the visual language used in that composition. Show typography, colour relationships, graphics, icons, imagery where useful, surfaces, and effects through actual rendered specimens. These are visual explorations, not production applications or full branding packages. Keep the user's task and content comparable across directions; composition, navigation, density, shapes, and visual language are open to change.

The studies should feel designed, not like an inventory of empty boxes. Supporting images may be generated or sourced when they add something specific. Use available image-generation tools for generated assets. Keep precise UI text and controls in HTML, and use a coherent real icon family. References are optional inputs, not a catalogue that limits possible styles.

Present the eight studies in the same neutral, full-window viewer with direction thumbnails, file thumbnails, and previous/next navigation. Read and copy only this viewer asset, not its sibling instructions:

`/Users/milad/Developer/argo/.claude/worktrees/ticket-1960-visual-skills/packages/argo-skills/skills/visual-exploration/references/viewer.html`

Follow its manifest format. Its embedded HTML has scripts disabled, so show UI states as static specimens. Use a separate local server port for your task. Keep the viewer separate from each direction's visual identity.

Inspect every rendered study at its intended desktop size and inspect the viewer at a narrow size. Record the actual file and view inspected, plus observed strengths and defects, in `inspection.md`. Judge legibility, asset presence, alignment, product usefulness, visual craft, and distinction between directions. Do not call something verified based on its source code or a repeated screenshot. Report remaining defects honestly. Return the preview URL and output path; leave selection to the user.
