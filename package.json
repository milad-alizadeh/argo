{
  "name": "argo",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "scaffold": "argo-skill-starter"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
