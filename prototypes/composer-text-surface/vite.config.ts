import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

// PROTOTYPE. Throwaway: four composer text surfaces on one route, switched by ?variant=.
export default defineConfig({ plugins: [react()] })
