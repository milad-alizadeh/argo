## Parent

#1937

## Objective

Extract the approved Session content into the production shell after #1939 is complete.

## Source of truth

Use the approved React prototype at commit `602bcce21d33d9d122fe3d2ae9bfaef33de1eb3c`. Its root component is `apps/desktop/src/renderer/modules/composer-prototype/ComposerPrototype.tsx` and its direct companion files.

Review the result against `Reference/Composer Prototype/Source Of Truth` in Storybook. Do not add placements, styles, or controls that the pinned source does not contain.

## Scope

- Extract the Roster and Feed states, including rich message content.
- Extract permission prompts and queued-message controls.
- Extract the attachment tray and composer controls.
- Extract context and usage controls.
- Extract the inspector lists for background Agents and Shells.

## Constraints

- Keep the committed `ComposerPrototype` unchanged.
- Preserve the shell and pane behavior from #1939.
- Do not restore the removed legacy Session UI.
- Keep production behavior behind the focused Session feature tickets in #1937.
- Use fixture-friendly visual surfaces and Storybook behavior stories where interactions are present.

## Acceptance criteria

- [ ] The production Session screen matches the approved Roster, Feed, composer, prompt, attachment, usage, and inspector surfaces.
- [ ] Each extracted component has Storybook stories for its named states.
- [ ] Story `play` tests cover visible interactions, keyboard routes, and focus behavior.
- [ ] The extracted surfaces preserve light and dark semantic roles.
- [ ] No fixture data is presented as live Session behavior.

## Blocked by

- #1939
