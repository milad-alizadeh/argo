# Cockpit — component inventory

The build contract extracted from the settled study `cockpit.html` (per
`rules/design-studies.md` "Ship the inventory"). `/componentize-design` consumes this
file to scaffold the real components — names, tiers, and props below are frozen for any row
still at `spec`; the study's markup/CSS are disposable. Once a row ships, the code becomes
truth and this file follows it (see Status, below). Behavior — ribbon rules R1–R16, the session state
table S0–S11, and pane anatomy — lives in `cockpit-matrix.md`; this file covers only
*what* the components are.

Coverage: the study carries **31 distinct `data-component` regions**; every one appears
exactly once below. Recurring primitives the study renders without a `data-component`
attribute (the icon helper, dots, chips, buttons, tool/message/check rows, the finding
card, the screen itself) are included and marked **unnamed in study** — `/componentize-design`
should treat their names here as proposals to freeze.

Tiering follows `rules/ui-components.md`: atoms → molecules → organisms → screen,
grouped one table per tier.

## Status — which artifact is truth

The cockpit is built row by row over many tickets, so `cockpit.html` is authoritative for
some regions and stale for others. The Status column says which, per row:

| Value | Truth | What a change means |
|---|---|---|
| `spec` | The study region | Design change → edit `cockpit.html`. That is what the study is for. |
| `partial · <path>` | Both — code for what it renders, the study for the rest | Notes name what is still outstanding. |
| `built · <path>` | The component + its stories | Change made while implementing → update this row's name/props. Leave the study region alone; it is frozen reference and may be stale. |

Paths are relative to `apps/desktop/src/renderer/src/components/`.

**A `built` row's name and props are whatever the code says**, not what the study proposed —
freezing happens at `spec`, and once a row ships the code wins. Where a build renamed a
study proposal, the Notes keep the old name so the study anchor stays findable.

**Only touch `cockpit.html` for a built region when the drift would mislead a still-`spec`
neighbour** — a changed shell, split ratio, or row height that unbuilt regions are laid out
against. Cosmetic divergence inside a built region is expected and costs nothing.

## Atoms

All atoms are unnamed in study (the study renders them via helpers/classes, not
`data-component` regions).

**Text is under everything below.** Wherever a Composed-of column names a word, a label, a
count, a path, a timestamp or prose, that string is rendered by the Text atom — the tables
name the copy, not a second way of setting type. The only exceptions are the two primitives
whose `asChild` hands their children to a Slot (Badge, Button); those compose the role class
from Text's map instead of wrapping.

| Component | Status | Props / variants | Composed of | Notes |
|---|---|---|---|---|
| Text *(unnamed in study)* | built · `ui/Text.tsx` | `{variant: headline \| title \| row \| row-strong \| prose \| meta \| tag \| eyebrow \| code \| code-inline, as?}` | — | The `.text-<role>` classes in `tokens.css`, specimened in `foundations.html`. The ONE way type is applied: every other component sets its type by rendering a Text, never by writing a role class. Colour is NOT part of a role — it composes through `className`. `as` picks the element, because the document outline never follows from a visual role. |
| Icon *(unnamed in study)* | built · `ui/icons/` | `{className, width?, height?, aria-hidden \| aria-label, role?}` — no `size` prop: the `.icon` box sets the default and a call site overrides with `size-*` | — | One file per glyph (`GitBranchIcon`, …), each `createIcon(PhosphorIcon)` — a Phosphor React component pinned to weight `light` in the shared `.icon` box, not the study's `<use>` sprite. Weight and fill never encode state; the enclosing element's `currentColor` does, so no icon file gets a say in either. Study anchor: helper `ic(n, x)` → `.ic`/`.ic.sm`. |
| StatusDot *(unnamed in study)* | built · `ui/StatusDot.tsx` | `{tone: run \| amber \| mist \| gray \| red \| stale \| landed, label?, pulse?}` — silent unless `label` names it | — | `.dot` + the shared `glow` class — fill and halo both read `currentColor`, so a tone sets one property. `--tone-landed` is promoted in `argo-tokens.css` (green pulled toward indigo). `.pulse1` is the screen's ONE animation budget (`--time-pulse`) — at most one pulsing element per render. |
| ContextGauge *(unnamed in study)* | built · `ui/ContextGauge.tsx` | `{percentage}` | Text `tag` "Context" label + `.ctxbar` fill (glowing, `currentColor`) + Text `meta` `~n%` | `.ctxrow`. Rail-only: the ONE shape for "context remaining"; the session header never repeats it. The estimate reads as plain text — provenance lives in `title`, never in decoration. `percentage` is estimated, so the gauge rounds and clamps it to 0–100. Track height is `--space-tight` (the contract's 4px rhythm step), not a bespoke 5px. |
| Badge *(unnamed in study)* | built · `ui/badge.tsx` | `{variant: neutral \| warn \| primary \| verdict-block \| verdict-changes \| verdict-approve, shape: default \| pill, asChild?}` + children (optional leading Icon) | — | `.badge` (`shape: default`) and `.schip` (`shape: pill`) are ONE atom — same `tag` role, same uppercase, same 40%-border / 12%-wash tint recipe, differing only in radius and padding. The mini bordered label: worktree / main tree, amber "uncommitted", primary "declared intent", "draft"; the pill is the chip a review finding's state is reported in, `--radius-full` over the `--verdict-*` ink + `--verdict-*-tint` pairs. Vendored shadcn `badge.tsx` with its variant map re-bound to these tones; the `tag` role supplies the uppercase, so the label is authored lower-case. Variant names are token names — which state wears which tone is `ui/findingState.ts`'s call, never this map's. |
| Button *(unnamed in study)* | built · `ui/button.tsx` | `{variant: primary \| ghost \| review-secondary \| verdict-changes \| verdict-approve, size: default \| sm, asChild?, disabled}` | Icon + label | Vendored shadcn cva with the stock variant map replaced by the cockpit's roles: `.mbtn` (gradient primary — drawer controls only, R2: one primary on screen) → `primary`, `.mbtn.ghost` → `ghost`, `.rv-btn.ghost` (Review-tab controls, all secondary) → `review-secondary`, `.abtn`/`.abtn.confirm` → the verdict tones with `.abtn.sm` → `size: sm`. The study puts `.abtn`, `.rv-btn` and `.mbtn` in one focus rule (L451), so they are one primitive. `ghost` also covers `.abtn.reopen`, which it matches at rest; the study's extra 4% hover wash there is dropped rather than restyle every ghost control. `ghost` is the default so a caller opts into the screen's one primary. Tones are token names — the finding vocabulary lives in `ui/findingState.ts`. |
| SectionHeader *(unnamed in study)* | built · `ui/SectionHeader.tsx` | `{label, count?}` | Text `eyebrow` + `.n` count (no trailing aside — the count is the only slot) | `.sect-h` — uppercase eyebrow with optional neutral count ("Outcomes · 4", "Agents · 1 live", "Checks · sha"). |
| PaneSplitter *(unnamed in study)* | built · `ui/PaneSplitter.tsx` | `{orientation: v \| h, size, min, max, invert?, label, onResize, className?}` | — | `.vsplit`/`.hsplit`; hover/drag highlights the pip, arrows step it by one rhythm unit. The study's `target: rail \| detail \| term` was the name of the custom property it wrote — `--c-rail`/`--c-act`/`--r-term` are **screen-local layout state, not tokens**, so the atom stays pure and reports a clamped px `size`; SessionScreen owns the properties. |

## Molecules

| Component | Status | Props / variants | Composed of | Notes |
|---|---|---|---|---|
| Status *(unnamed in study)* | built · `ui/Status.tsx` | `{word, tone, pulse?}` | Text `meta` word + StatusDot | `.stw`/`.aword`/`.sword` + `.dot` — the lifecycle word and its dot are one unit and one order: **word first, dot terminating the row**. Word and tone arrive already derived, never typed at a call site, so a state cannot be spelled two ways on two surfaces — and the same two props carry a ship-flow word (`Ready to merge`), which a state key could not. The word carries the state and the dot only tints it (colour never carries state alone, a11y), which is why the dot is decorative here. A dot with no word beside it is the StatusDot atom instead. The vocabulary itself — `SESSION_STATUS` (state → word · tone · icon name) and the `RailTone` union — lives in `ship/railStatus.ts` beside `railStatus()`; adding a state means adding it there. The UI holds only the presentational halves: `STATUS_TONE` (tone → colour class, `ui/statusTone.ts`) and `RAIL_ICON` (icon name → atom, `ui/icons/railIcon.ts`). |
| WorkspaceIdentity | built · `ui/WorkspaceIdentity.tsx` | `{branch, tree: main \| worktree, directory, dirty, ahead, behind, sharedCount, className?}` · tag variants: `main tree` / `worktree` (directory leaf matches branch leaf) / adopted `worktree <directory>`; dirty chip (amber); sync `↑a ↓b vs main` or `= vs main`; shared-tree warning | Icon (git-branch), Text `code-inline` branch, Badge, StatusDot, Text `meta` sync line | `.wschip`, fn `wschipHtml`. The ONE full home of "where is this session working" — the rail row only echoes branch+tree. Branch is typed exactly once on screen, here. **Names are spelled out** (`worktree`, `directory`) per `rules/typescript-style.md` — the study's tag text is the same word; the earlier `wt` spelling in this row and in `cockpit-matrix.md` was the drift and is gone. The `↑a ↓b` arrows are git's own ahead/behind notation carried as text, **not** glyphs standing in for icons — the one sanctioned exception to `rules/ui-components.md`'s glyph ban, recorded here rather than defended in the source. Vertical padding snaps to `hair` (2px): the study's 3px is off the spacing ladder, so all four tag variants sit at a constant 22px rather than the study's 24px. The badge's spelled directory takes the mono family only, never the `code-inline` role, so it cannot grow the badge past its siblings. An empty `directory` classifies as adopted and renders a tag naming nothing — invalid input to guard at the container that feeds this, not a state to render. |
| NowLine | built · `ui/NowLine.tsx` | `{icon, line (ReactNode, may embed `code-inline` spans), live: boolean, className?}` | Icon + Text `row-strong` line + Status (`live`) | `.nowstrip`/`.now`, fn `nowHtml`. Quiet one-line strip; "live" is the Status molecule with no pulse. The line is `row-strong` per the study's `.now .line` — this row previously said `row`, and the study won. `live` is a word the design fixes, not a session state, so it is the one word a molecule supplies itself (see `Status`'s `word` TSDoc). The study's `.inset` inner top-highlight is dropped, following `SessionRow`. |
| CheckRow *(unnamed in study)* | spec | `{key, label: Lint \| Typecheck \| Tests, state: pass \| fail \| warn \| run \| none \| queued \| partial, valueLabel, derived, selected}` | check glyph + label + Text (estimate, provenance in `title`) + hover "rerun" | `.chkrow`, fn `chkRow`. Click opens the captured output in the Commits drawer (R11), never a pane. Glyph tones: `--tone-done` / `--tone-red` / `--tone-amber`; value text: `--signal-*`. |
| OutcomeRow | spec | `{type: plan \| feature \| fix \| refactor \| test \| docs \| review \| research \| chore \| untyped, title, prov, life: run \| done \| queued \| fail \| interrupted \| needs-input, files[], artifacts[], selected}` | type icon + type word + title + neutral rollup (`n files +a −d · n artifacts`) + Status | `.orow`, fn `outcomeRow`. Rollup renders neutral — colour is lifecycle-only. A `review`-type row routes to the Review tab instead of scoping the Work pane. |
| MessageRow *(unnamed in study)* | spec | `{say, status: run \| done, decl?, at?, tools[], expanded}` | caret + Text `prose` + Badge (decl) + Text `meta` timestamp + StatusDot + folded ToolRows | `.msg`/`.msg-head`, fn `msgHtml`. The Timeline's item: agent prose is the spine, tool rows fold underneath; `at` = transcript clock time (DIRECT tier), right-aligned. One-line rows truncate, never wrap (full text in `title`). |
| ToolRow *(unnamed in study)* | spec | `{icon, label (html), running, dv, rmeta, kind: 'test' (+ exit, pass, fail), add/del, noCapture, chref, selected}` | Icon + label + Text estimate (provenance in `title`) / pass-fail verdict / `+a −d` | `.lrow`, fn `toolRow`. One-line prose (R12); click opens the captured feed as a console channel (R13). `chref` rows are dispatch mentions — they open the actor's channel; the roster alone owns agent state (R15). An estimated value (`dv`) renders as plain text with its provenance in `title`; only `noCapture` — a value the tool never reported — dims (`.unreported`). |
| AgentRow | spec | `{name, goal, state: running \| done \| failed \| queued, duration, channelId}` | sparkle Icon + Text `row-strong` name + Text `row` goal + trailing meta unit (Status + Text `meta` duration as one faint span; state hue on the word only) | `.lrow.agent`, fn `agentRow`. An Agent is a PROCESS, not a tool call. Status word renders only where it informs: running/failed always; done/queued only when differing from the enclosing phase's rollup (lone rows always). Static — no pulse. |
| RunRow | spec | `{label, shape: batch \| pipeline, state: running \| done \| failed, duration, members: AgentRow[], phases: [{lbl, st: done \| run \| wait, n?}]?, open}` | caret + tree Icon + name + shape word + collapsed-only progress summary + members in `.runkids` (flat for a batch, PhaseGroups for a workflow) | fn `runRow`. Shape UI copy: `batch` \| `dynamic workflow` (model keeps `pipeline`; never that word in UI). Progress (`Survey ✓4 → …` / `n/m done`) renders only while collapsed — expanded, phase rails + members own it. |
| PhaseGroup | spec | `{runId, label, state: done \| run \| wait, members: AgentRow[], open}` | state-tinted left rail + caret + eyebrow phase name + inline `pstat` (`done 4/4` / `running 1/3` / `queued`, rail-hued, directly after the name) + indented member AgentRows | `.phgroup`/`.lrow.phead`, fn `phaseGroups`. A Phase is a grouping, not an Actor. Rail and pstat share one hue per state (working = running, green = done, faint = queued); the right edge stays a single faint duration column. Done phases collapse to the header, the active phase defaults open, future phases are header-only. |
| RibbonNode | built · `work/RibbonNode.tsx` | `{nodeKey: commits \| pr \| ci \| review \| merge, state: wait \| now \| gate \| sync \| auto \| done \| fail \| warn \| stale \| lock, sub?, isHead, open, clickable}` | glyph disc + label + sub echo | `.rnode`. `key` → `nodeKey` (React-reserved). `nodeKey`/`state` import `RibbonNodeKey`/`RibbonNodeState` from `@shared` rather than re-declaring the union. Gate ≠ in-progress by FORM (outer ring), not tint — `now`/`gate`/`sync` share one glyph in `work/ribbonNodeState.ts` (state → icon + tone; primary+verdict tones, not `RailTone`) and the ring is the component's own addition for `gate`. `stale` = dashed + strikethrough (fact about an older sha, R3). `auto` = delegated standing order (R6). Head gate/fail/warn glyph takes the one `.pulse1` (R10), reusing `animate-pulse-status`. Sub is a count/sha echo, never a re-typed value — this ticket doesn't derive it (#33), so no caller passes one yet. |
| PrAnchor | built · `work/PrAnchor.tsx` | `{prNum, ghUrl}` | `Text code-inline` `PR #n` + `ArrowRightIcon` + `Text code-inline` `main` + `Button asChild` GitHub link | `.shipanchor`. Exists only once a PR does (R9); PR surfaces show only "→ main" — the branch lives in WorkspaceIdentity. `→` resolves to the new `ArrowRightIcon` atom (`ui/icons/`) rather than the WorkspaceIdentity `↑a ↓b` text exception. |
| CiCard *(unnamed in study)* | built · `work/CiCard.tsx` | `{heading (ReactNode), trailing? (ReactNode), children, className?}` | bordered card shell + header row + body slot | `.ci`. Added mid-build (not a `data-component` region of its own) — the shared shell PrChecksList and CheckOutput both compose, per the componentization audit. Heading/trailing are caller-composed `Text`, the `NowLine.line` pattern, so the shell stays agnostic between "sha + aggregate" and "command" content. |
| PrChecksList | built · `work/PrChecksList.tsx` | `{sha, status: CiStatus (@shared), aggregate, runs: readonly CiRun[]}`, `CiRun = {name, status: queued \| running \| passed \| failed, duration, note?}` | `CiCard` (heading: "CI · GitHub Actions · sha", trailing: aggregate) + run rows (`StatusIcon` + mono name + note + time + ghost "open" hint) | `.ci`, fn `ciDrawerHtml`. Remote-origin CI, deliberately origin-labelled; every row deep-links to its run once a later ticket wires it (no `onClick`/url yet — presentational only). NOT the local Checks card. `agg`/`cls`/`st`/`t`/`n` spelled out per `rules/typescript-style.md`. Run-row glyph reuses `StatusIcon` off the rail's own `RailIcon`/`RailTone` vocabulary (four states map cleanly: queued→circle/gray, running→circle-notch/run, passed→check/done, failed→x/red) rather than a parallel icon set. |
| CheckOutput | built · `work/CheckOutput.tsx` | `{check: lint \| types \| tests, command, output}` | `CiCard` (heading: `Text tag` label + `Badge` "Argo · this tree", trailing: `Text code-inline` command) + `Text code` feed | fn `checkBlockHtml`. Local-check captured output, sha-keyed → renders inside the Commits NodeDrawer (R11), via `NodeDrawerSession.commits.selectedCheck?` (screen-level "which check is open" state isn't owned here). Shares the `CiCard` shell with PrChecksList. `cmd`/`out` spelled to `command`/`output`. |
| ChangesViewToggle | spec | `{view: all \| commits}` | two segmented `.vt` options | `.vtoggle`. Lives inside the WorkTabs strip, Changes tab only. |
| FileDiff | spec | `{kind: M \| A \| D \| R, path, adds, dels, commit \| null, hunk[], viewed, findings[]}` · variants: `viewed-on` (dimmed, path struck, hunk collapsed), uncommitted (`wipchip` in All-files view), finding flag with open count | header (kind Icon + Text `code-inline` path + Badge + flag + Text `meta` diffstat + Viewed checkbox) + hunk + inline FindingCards | `.fdiff`, fn `fileDiff`. Finding BODIES render inline under the hunk (R14); the Review tab jumps here. Header alone is the reusable shape if the app splits it. |
| FindingCard *(unnamed in study)* | spec | `{severity: blocking \| advisory, state: open \| addressing \| fixed, line, body, by, walkFocus}` | sev glyph (▲/◆) + severity word + `:line` + pill Badge + Button (sm) + body | `.finding`, fn `findingCard`. Verdict-tinted card wedged under a hunk; `resolved` collapses to a one-line stub. Distinct from FindingRow (the Review-tab list row) — same finding, two homes. Reads the chip's and the control's word, glyph, tone and tooltip from `ui/findingState.ts`. The card is itself clickable (it routes to the diff), so it — not the Button primitive, which must never swallow a click on every caller's behalf — stops propagation on the control. |
| FindingRow | spec | `{id, file, line, severity: blocking \| advisory, body, by, state: open \| addressing \| fixed, focus}` | sev glyph + mono `file:line` + "diff" jump + body + attribution + pill Badge + Button | `.rvrow`, fn `reviewFindingRow`. `fixed` dims (opacity + green edge); `rv-focus` highlights after a jump. Click routes to the diff, so the row stops propagation on the control the way FindingCard does. Chip and control read `ui/findingState.ts`. |
| ReviewVerdictHero | spec | variants `changes` / `approve`: `{heading, blockingLeft, summary, attribution: 'argo · code-review' + base sha, note}` | vglyph (binoculars disc) + title + prose + attribution + advisory note | `.rv-hero`. The SOLE home of the agent-verdict prose; the outstanding count lives only in the Review tab badge. "Blocking is advisory — Argo holds no merge gate." |
| CommitGroup *(unnamed in study)* | spec | `{sha \| 'wip', msg, files: FileDiff[], wip}` | group header (mono sha + strong msg, or amber "Uncommitted changes") + FileDiffs | `.cgroup`, fn `groupedFiles` — the By-commit view of the Changes tab. |
| ArtifactCard | spec | `{path, body (html), missing, open, producedBy: outcome}` · variants: open / collapsed / `gone` (path struck, warning icon, "no longer on disk") | caret + Icon + mono path + producing-outcome echo + collapsible `.doc` body | `.acard`, fn `artifactCard`. Every artifact is tagged with the outcome that produced it. |
| ConsoleChannelTab | built · `console/ConsoleChannelTab.tsx` | discriminated union — shared `{id, label, active?, panelId?, onSelect}` + `live` `{kind}` / `capture` `{kind, agent?, onClose}` | (sparkle Icon) + label + close ✕ | `.chtab`. `session · live` is fixed and uncloseable — that rides on the TYPE, not a convention: only the `capture` arm has an `onClose` to give a ✕, and only it carries the sparkle. Captures are timestamped (`vitest @12:04`) and caretless. The chip holds TWO controls, so it is not itself a `<button>`: the wrapper wears `buttonVariants({variant: 'quiet', size: 'sm'})` and hands its semantics off with `role="presentation"`; both children are real `Button`s (`variant: 'bare'`, `size: 'none'`) so the focus ring comes from the ladder rather than being re-typed. `quiet` is a new Button variant — borderless, its selected wash keyed off `data-active`; `bare`/`none` are the nested-control rungs. The label truncates so a long tool name cannot push the strip's controls out of the console. |
| ConsoleChannel | built · `console/ConsoleChannel.tsx` | discriminated union — shared `{id?, ref?}` + `live` `{kind, prompt, tail}` (contenteditable, prompt + static caret) / `capture` `{kind, feed}` (escaped feed, "esc returns" note) | mono feed + caret / note | `.term-body`. The caret is static — the animation budget belongs to the ribbon/rail — and is one monospace advance wide by the study's settled 14px (`--caret-box-w`/`--caret-box-h`/`--caret-baseline` + the `caret-cell` utility). `prompt`/`tail`/`feed` are the View's way of receiving what the study hard-coded. The feed is rendered as text, never markup; a leading `▸` is lifted off and tinted (`feedLines`) — captured data, not an icon. `id`/`ref` let the Console link the panel to its tab and pull focus onto it. |

## Organisms

| Component | Status | Props / variants | Composed of | Notes |
|---|---|---|---|---|
| SessionRow | partial · `rail/SessionRow.tsx` | shipped: `{session: SessionView}` → state Icon + title + status word + StatusDot + CLI meta line. **Outstanding**: `slot` (doing-now), `ctx` (ContextGauge), the model · tokens · duration meta tuple, the `ws` workspace echo, `selected`, `pulse`, and the ribbon-derived status words (`railStatus`) | state Icon + Text `row-strong` title + Status / doing-now slot / workspace echo line / ContextGauge / meta line | `.srow`. The rail triage row — SOLE home of the Context·model·tokens·duration tuple. Status words are R16's table in `cockpit-matrix.md` — spelled there once and nowhere else. Ribbon-derived status is a POINTER into the head node, never a value. Shared-tree echo goes amber. Top needs-you dot takes `.pulse1` when the ribbon is quiet. |
| SessionRail | partial · `rail/Rail.tsx` | shipped: `{sessions[]}` → one frosted panel, a "Sessions" eyebrow, scrolling SessionRows, EmptyRail when the roster is empty. **Outstanding**: `selIdx`, the "Projects" header (+), the collapsible project row, `projOpen`, the Concierge dock and `orbState` | "Projects" header (+) / collapsible project row ("argo", + new session) / scrolling SessionRows / Concierge dock (canvas orb + state word) | `.panel.rail`, fn `railHtml`. Orb + sky glow are decorative canvas singletons (component-local colours, not tokenized). **Built as `Rail`**, not `SessionRail` — the study name is kept here as the anchor. |
| EmptyRail | built · `rail/EmptyRail.tsx` | — | compass Icon + Text `row` "No Sessions observed yet." | **Not in the study** — the study assumes a populated rail. Added with the rail itself (#3); the empty roster is a real state, so it gets a row. |
| SessionHeader | spec | `{project, title, ws, showDetail}` | breadcrumb (`argo › title`) + WorkspaceIdentity + "Work" toggle `.tg` | `.shead`, fn `sessionHtml`. |
| BackgroundTasks | spec | `{actors: (AgentRow \| RunRow)[]}` | SectionHeader ("Background Tasks" — name alone, no counts) + inset list of RunRows/AgentRows | fn `agentsHtml`. The Story pane's Actor roster (R15) — the roster alone owns agent state; each agent's stream is its console channel. |
| BranchChecksCard | spec | `{latestSha, uncommittedCount, checks: {lint, types, tests}}` | SectionHeader ("Checks · sha +n uncommitted") + three CheckRows | fn `checksHtml`. LOCAL checks only, keyed to the sha — remote CI is a different fact and lives on the ribbon's CI node; the agent review's one pointer is the Review tab badge. |
| OutcomesList | spec | `{outcomes[]}` (sorted active-first: run/fail/interrupted/needs-input → done → queued) | inset of OutcomeRows | fn `summaryHtml`. The "what got done" spine of the Story pane. |
| Timeline | spec | `{messages: MessageRow[]}` | "Timeline" peer strip (name alone) + inset of MessageRows (ToolRows folded) | fn `narrHtml`. The agent's own prose is the spine; raw I/O routes to the Console, sha-keyed product to the Work pane. |
| PrLifecycleRibbon | built · `work/PrLifecycleRibbon.tsx` | `{model: RibbonModel \| null (@shared — field is `nodes`, not `st`), openKey: RibbonNodeKey \| null, pr: {num, ghUrl} \| null}` · variants: five-node strip / `terminal` (Merged `landed` / Closed word replaces the nodes, R8) / absent (R7: `model` is `null`) | RibbonNodes (Commits · PR · CI · Review · Merge) + PrAnchor | `.shipbar`, fn `shipbarHtml`. The Work pane's header and the ONE home of ship-flow state. Re-derives nothing: `model` is the shared `RibbonModel` `ribbonModel()`/`shipState.ts` already produce — head, per-sha staleness and terminal are read off it, never recomputed. Head = leftmost non-fresh node (R1). Every node state is a fact about a sha, so done can go stale (R3). Root element is a `<section aria-label="Work">` (a bare `div` fails the a11y-supported-aria-props lint for `aria-label`). |
| NodeDrawer | built · `work/NodeDrawer.tsx` (+ colocated `nodeDrawerModel.ts`) | `{node: RibbonNodeKey, state: RibbonNodeState, isHead, session: NodeDrawerSession} \| {node: 'terminal', state: TerminalState, session}` — a discriminated union on `node` so `state`'s type narrows per branch; `session` bundles the per-node display facts the ribbon's own state doesn't carry (draft message, CI runs, review rounds, merged/closed summaries) · bodies: commits (`now` narrated / `gate` draft-msg + Commit / `sync` Push ↑n / done echo, plus `CheckOutput` when `selectedCheck` is set) · pr (`gate` two-step Create PR → main (R4) / `auto` + revoke / done) · ci (PrChecksList + Fix CI / Re-run when head and failed) · review (round-stacked GitHub rounds + Address n when head) · merge (`gate` two-step Merge / `auto` + revoke / `lock` stale-upstream / wait) · terminal (merged / closed summaries + next-step ghost) | drawer heading + `.growrow` status lines + `.draftmsg` + Buttons + PrChecksList + CheckOutput | `.rdetail`. A drawer carries a control ONLY when its node is the head (R2) — the screen's one primary lives here. Gate actions are two-step confirm, built on the promoted `useDisclosure` (`ui/useDisclosure.ts`, out of the unmerged `components/story/`); delegated gates narrate `auto ⚙` with revoke (R6). Narrated states reuse the `Status` molecule. Buttons render unwired (no `onClick`) — dispatch is a later ticket's seam, matching the props frozen here. |
| WorkTabs | spec | variants: unscoped `{tab: changes \| review \| artifacts, counts, reviewBadge: {outstanding, glowDot} \| ✓}` · scoped `{outcomeTitle, back: "‹ All changes"}` · stub (non-mocked sessions: static Changes/Artifacts) | `.dtab`s + Review badge (count + static `.tglow` dot) + ChangesViewToggle (changes tab only) | `.dtabs`, fn `detailHtml`. Same 41px strip height as the ribbon; the strip IS the pane header (region names live in aria-label only). The Review tab badge is the agent review's ONE pointer (R14). |
| AllFilesDiff | spec | `{files: FileDiff[]}` (+ pane-level "vs merge-base · +a −d · uncommitted included" SectionHeader) | FileDiffs with wip marking | fn `allFilesHtml`. ONE flat cumulative diff vs merge-base — net effect, not per-commit concatenation. The By-commit sibling view renders CommitGroups. |
| FindingsList | spec | `{findings: FindingRow[]}` | SectionHeader ("Findings" eyebrow) + FindingRows | `.rv-list`, fn `reviewTabHtml`. Sits under the hero and an `.rv-cta` row (Address n findings / Re-review — both secondary buttons); no merge button and no header count here. |
| ArtifactsList | spec | `{artifacts: ArtifactCard[]}` | ArtifactCards | fn `artifactsTabHtml`. Every artifact of the session, tagged with its producing outcome. |
| ConsoleChannelTabs | built · `console/ConsoleChannelTabs.tsx` | `{capture?, activeChannel, expanded, panelId?, onSelectChannel, onCloseCapture, onToggleExpanded}` | live ConsoleChannelTab + at most ONE capture ConsoleChannelTab + expand/collapse control | `.term-h`, fn `consoleHtml`. One capture slot (R13): opening a feed replaces it — no tab accumulation; ✕ clears. Esc (or the live tab) returns to `session · live`. The one-slot rule is carried by the TYPE — `capture` is a single optional value, never a list — so no surface can accumulate tabs. A selection naming a capture that was replaced or cleared falls back to live (`resolveActiveChannel`) rather than lighting nothing. The `role="tablist"` wraps the tabs ALONE — the expand/collapse control is its sibling, because only tabs may be a tablist's children — and the selected tab points at the panel through `panelId` (the Console owns both ids). |
| Console | built · `console/Console.tsx` | `{live, capture?, activeChannel, expanded, height, onSelectChannel, onCloseCapture, onToggleExpanded}` | ConsoleChannelTabs + ConsoleChannel | `.term`. The ONE monospace surface (R13); raw tool/agent feeds fill the single capture slot, never a pane. `height` is a CSS **length** the screen passes in (`var(--r-term)`) — screen-local layout state, NOT a token: SessionScreen owns the property its splitter drives and the Console never knows its name. `live` carries what the study hard-coded (prompt + tail). Esc is bound on the region rather than the window, so the console answers only for keys pressed inside it — and because R13 opens a capture from a TIMELINE row (focus outside the console), the Console pulls focus onto a capture panel when it becomes active. That is what makes the panel's own "esc returns" note true; a screen-wide Esc stays the screen's. If the live channel ever becomes a real xterm pane it MUST import `@xterm/xterm/css/xterm.css`. |

## Screen

| Component | Status | Props / variants | Composed of | Notes |
|---|---|---|---|---|
| SessionScreen *(unnamed in study)* | partial · `App.tsx` | shipped: the container only — `App` subscribes the projection bridge and renders `Rail` as the whole window. **Outstanding**: the split into container / View, and every prop here — `{sessions[], selIdx, showDetail, detailTab, changesView, ribbonSel, checkSel, activeChannel, channels, expanded/viewed/artOpen/runOpen maps, findingStatus, landStep, termTall}` · variants: Work pane hidden (`solo`), scoped Work pane (outcome product), terminal ribbon, ribbonless (R7) | SessionRail ‖ PaneSplitter ‖ session panel { SessionHeader / Story region (NowLine + BackgroundTasks + BranchChecksCard + OutcomesList + Timeline) ‖ PaneSplitter ‖ Work region (PrLifecycleRibbon + NodeDrawer + WorkTabs + tab pane: AllFilesDiff \| CommitGroups \| ReviewVerdictHero + FindingsList \| ArtifactsList) / PaneSplitter / Console } | `.app`/`.session`, fns `render`/`sessionHtml`/`workHtml`. Regions carry aria-labels ("Story", "Work") only — no pane title rows. The sky backdrop (stars canvas + glow) is decorative. In the app this splits container (state + wiring above) / View per `ui-components.md`. |
