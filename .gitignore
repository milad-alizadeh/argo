node_modules/
.turbo/
dist/
*.log
.DS_Store

# Installed skill copies are throwaway outputs of `npx skills add` —
# skills-lock.json is committed and restores them via `npx skills experimental_install`.
.agents/
.claude/*
# House rules are authored, not installed — keep them tracked.
!.claude/rules/

# graphify: commit the graph outputs (graph.json, graph.html, report, labels).
# Ignore only the internal rebuild cache — it's regenerable and would churn every commit.
graphify-out/cache/
