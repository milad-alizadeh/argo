node_modules
dist
out
.DS_Store
.eslintcache
*.log*

*storybook.log
storybook-static

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Visual regression: only the Linux baselines CI generates are committed. A local
# (macOS/Windows) run writes platform-suffixed images — never commit those.
**/__screenshots__/**/*-darwin.png
**/__screenshots__/**/*-win32.png
# Vitest visual-diff attachments (actual/diff images, written on failure).
.vitest-attachments/
