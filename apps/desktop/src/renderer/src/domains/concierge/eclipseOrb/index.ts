// The eclipse-orb engine's public entry — the factory plus its handle types.
// Implementation lives in the named leaf (createEclipseOrb.ts); this is wiring only.
export { createEclipseOrb } from './createEclipseOrb'
export type { OrbHandle, OrbOptions, OrbState } from './types'
