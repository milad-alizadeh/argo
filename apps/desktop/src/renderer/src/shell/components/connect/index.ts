// The connect panel's parts. Only the organism and its handler type leave the folder: the
// rows, the device code and the grant notice are its own, and Project Settings reaches the
// same organism rather than a second one.

export { ConnectPanel, type ConnectPanelHandlers } from './ConnectPanel'
