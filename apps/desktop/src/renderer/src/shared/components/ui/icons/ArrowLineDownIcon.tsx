import { ArrowLineDownIcon as PhosphorArrowLineDownIcon } from '@phosphor-icons/react'
import { createIcon } from './createIcon'

export const ArrowLineDownIcon = createIcon(PhosphorArrowLineDownIcon)
