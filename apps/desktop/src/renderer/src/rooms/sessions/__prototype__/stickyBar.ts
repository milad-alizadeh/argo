// PROTOTYPE. The one sticky-bar recipe the synthesis wears everywhere — the turn seams and the
// delegate scope's way back — so the surface's only standing chrome always reads as one thing.
// Solid panel under it, because content scrolling through a translucent bar is the clutter the
// variant exists to remove.
export const STICKY_BAR =
  'sticky top-0 z-10 -mx-region flex items-baseline gap-snug border-b border-b-inset-hair bg-background/90 px-region py-snug backdrop-blur-md'
