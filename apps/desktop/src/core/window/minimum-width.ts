// The narrowest window that holds the rail, the sidebar and the content at their minimums, plus the
// 1px resize handle between the last two. Narrower, the panel group squeezes the sidebar below its
// own minimum and its content spills out.
export const WINDOW_MINIMUM_WIDTH = 689
