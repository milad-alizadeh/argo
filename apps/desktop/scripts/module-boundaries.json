{
  "_comment": "LLM-MAINTAINED MODULE MAP for @argo/desktop. Single source of truth for module boundaries; dependency-cruiser.cjs turns it into lint rules mechanically — edit this map, never the generated rules. Refresh when the src/ structure changes. Managed by the setup-module-boundaries skill.",
  "tsConfig": "tsconfig.web.json",
  "exclude": "(^|/)(node_modules|out|dist|build|coverage|test-results|playwright-report|\\.storybook|e2e)/",
  "orphansAreWarnings": true,
  "modules": [
    {
      "name": "shared",
      "comment": "Cross-process contract: the Session state list and the union derived from it, the locked runtime tree (Agent · Subagent · Turn · Tool Call · Plan · Compaction · Usage), the Session class/posture axis, the delivery facts main observes, the event vocabulary, projected-state types, IPC channel names, the pure projection reducer, and the lifecycle derivation (session facts → delivery render state). Types, frozen constants and pure functions only — no I/O, no framework. `SessionView` carries facts, never a rendered state, so grading stays on the renderer's side of the bridge. Imported by all three processes (renderer files reach it through the `@shared` alias), imports none of them.",
      "path": "^src/shared/",
      "publicEntry": ["^src/shared/index\\.ts$"]
    },
    {
      "name": "main",
      "comment": "Electron main process. Its internals are private; the app is wired only from its own entry.",
      "path": "^src/main/",
      "publicEntry": ["^src/main/index\\.ts$"]
    },
    {
      "name": "preload",
      "comment": "The context-bridge. Loaded by Electron via file path, not imported — nothing should import its internals.",
      "path": "^src/preload/",
      "publicEntry": ["^src/preload/index\\.ts$", "^src/preload/index\\.d\\.ts$"]
    },
    {
      "name": "renderer",
      "comment": "The UI (browser context). Talks to main only through the window bridge, never by import.",
      "path": "^src/renderer/",
      "publicEntry": ["^src/renderer/src/main\\.tsx$"]
    },
    {
      "name": "renderer-shared",
      "comment": "The renderer's shared leaf (feature-sliced `shared/`): the framework-primitive kit (`components/ui` + its icons) that every slice composes, and the status presentation vocabulary (`shared/status`: the Roster status word/icon and the `deliveryState` composition over the `@shared` lifecycle derivation). The folder is named for what it holds — the status word/tone vocabulary every room reads — and not for any one room's use of it. A building block imported by two or more domains lives here, so every domain may import it and it imports no domain in return. External code reaches it only through the three barrels; its internals are private.",
      "path": "^src/renderer/src/shared/",
      "publicEntry": [
        "^src/renderer/src/shared/components/ui/index\\.ts$",
        "^src/renderer/src/shared/components/ui/icons/index\\.ts$",
        "^src/renderer/src/shared/status/index\\.ts$"
      ]
    },
    {
      "name": "cockpit",
      "comment": "The composition root's WIRING: the hooks that read the Zustand store and the preload bridge and assemble each slice's props, plus the stage that picks the active room's component. Slices are pure Views (rules/ui-components.md, container/View: 'the View never imports data access'), so this is the one place on the renderer side where the store and the bridge are read — which is why this wiring cannot live inside `shell/` even though most of it serves the shell. It may import every slice's public entry; no slice may import it, so the dependency runs root → slice and never back. Its own files stay behind `cockpit/index.ts`, which `App.tsx` is the only caller of.",
      "path": "^src/renderer/src/cockpit/",
      "publicEntry": ["^src/renderer/src/cockpit/index\\.ts$"]
    },
    {
      "name": "shell",
      "comment": "The shell slice (cockpit-spec.md §11.2, inventory in docs/designs/cockpit-ui-inventory.md): the project strip, the one merged floating top bar and its four regions (orb seat, connection chip, room tabs, global git group), the ⌘K palette, the keymap, the empty first-run seam, the connect/Project-Settings panel, spawn and navigation. Chrome renders in all three rooms or it is not chrome. A peer of the room slices, never their parent. The renderer root composes shell + active room, so the shell reaches into no room and no room reaches into it.",
      "path": "^src/renderer/src/shell/",
      "publicEntry": ["^src/renderer/src/shell/components/index\\.ts$"]
    },
    {
      "name": "rooms-sessions",
      "comment": "The Sessions room (⌘1, home): the roster rail and the session interior: header, the two-pane Activity surface, Delivery, and the always-on Dock. On the locked runtime vocabulary (Agent · Subagent · Turn · Tool Call · Plan · Workspace · Compaction · Usage), not the retired Run/Phase/Actor names. Composed by the renderer root through its barrel; its internals are private and it never reaches into another slice.",
      "path": "^src/renderer/src/rooms/sessions/",
      "publicEntry": ["^src/renderer/src/rooms/sessions/components/index\\.ts$"]
    },
    {
      "name": "rooms-work",
      "comment": "The Work room (⌘2): the list rail (Next-up hero over a priority hierarchy), the generic node tree where type is a property rather than a schema, and the two-pane ticket detail. Renders the Work Item provider's own status word verbatim; the canonical bucket is ranking/filtering only. Composed by the renderer root through its barrel; its internals are private and it never reaches into another slice.",
      "path": "^src/renderer/src/rooms/work/",
      "publicEntry": ["^src/renderer/src/rooms/work/components/index\\.ts$"]
    },
    {
      "name": "rooms-code",
      "comment": "The Code room (⌘3), session-independent: the file explorer over the primary checkout, name-or-content search, the lightweight editable editor, the scratch terminal attached to no agent, and the degraded file states. Composed by the renderer root through its barrel; its internals are private and it never reaches into another slice.",
      "path": "^src/renderer/src/rooms/code/",
      "publicEntry": ["^src/renderer/src/rooms/code/components/index\\.ts$"]
    },
    {
      "name": "delivery",
      "comment": "RETIRED (#157; disposition in docs/designs/cockpit-ui-inventory.md). The Delivery Panel (CONTEXT.md region; supersedes ADR-0009's `work/`): the Delivery lifecycle and node drawers — DeliveryLifecycle + NodeDrawer, the CI card molecules, the LifecycleNode state→icon/tone vocabulary. Pure Views over the pre-derived `LifecycleModel` from `@shared`. A feature slice composed by the screen through its barrel; its internals are private and it never reaches into another domain.",
      "path": "^src/renderer/src/domains/delivery/",
      "publicEntry": ["^src/renderer/src/domains/delivery/components/index\\.ts$"]
    }
  ],
  "layers": {
    "_comment": "Two isolations. (1) Electron process isolation: the three processes must never import each other — the only channels are the preload bridge (renderer↔main) and Electron's own preload wiring (main→preload by path). All three may import `shared` (the cross-process type/pure-function contract); `shared` imports none of them. (2) Renderer feature-slice isolation, unchanged in kind by the #157 redesign and now applied to the shell + room slices as well: a slice must not reach into another slice's internals (slices ⊥ slices, so shell ⊥ rooms and room ⊥ room); every slice may import `renderer-shared`; `renderer-shared` is a leaf that imports no slice. The shell is a PEER of the rooms, not their parent: the renderer root composes shell + active room outside every slice, exactly as `CockpitScreenView` composes them today. One retired domain keeps its rules until its last file moves (`activity` is gone: #261 retired the Run/Phase/Actor vocabulary it was named for; `roster` is gone: #267 moved the rail into `rooms-sessions`; `console` is gone: #268 moved the PTY view to `renderer-shared` as `TerminalPane` and deleted the channel strip with the panel). (3) The composition root's wiring (`cockpit`) is the only renderer code that reads the store or the bridge, and the dependency runs one way: it imports slices, no slice imports it.",
    "main": ["renderer", "preload"],
    "renderer": ["main", "preload"],
    "preload": ["main", "renderer"],
    "shared": ["main", "renderer", "preload"],
    "shell": ["cockpit", "rooms-sessions", "rooms-work", "rooms-code", "delivery"],
    "rooms-sessions": ["cockpit", "shell", "rooms-work", "rooms-code", "delivery"],
    "rooms-work": ["cockpit", "shell", "rooms-sessions", "rooms-code", "delivery"],
    "rooms-code": ["cockpit", "shell", "rooms-sessions", "rooms-work", "delivery"],
    "delivery": ["cockpit"],
    "renderer-shared": [
      "cockpit",
      "shell",
      "rooms-sessions",
      "rooms-work",
      "rooms-code",
      "delivery"
    ]
  },
  "placement": {
    "_comment": "WHERE FILES MAY LIVE, as opposed to what they may import. dependency-cruiser only sees edges, so a module's own file parked at that module's root — or a one-caller helper parked in the shared tier — passes every boundary rule while sitting in the wrong folder. These three blocks compile into the gates `scripts/root-files-check.mjs`, `scripts/earned-shared-check.mjs` and `scripts/kind-folder-check.mjs` (run by `bun run quality` and by pre-commit), which is what turns the prose in rules/file-structure.md into arithmetic. Each exemption is labelled KIND (the rule does not apply to that category) or RATCHET (debt; the list may only shrink), the same convention as biome.jsonc and scripts/jscpd-ignore-reasons.txt.",
    "rootFiles": {
      "comment": "What may sit LOOSE at each module's root — one entry per module, and a module with no entry FAILS (ADR-0021). The predecessor gate guarded one hardcoded path, so an unlisted module was silently exempt; every module added after it inherited that exemption and flattened, which is the whole ratchet below. `path` overrides the root pattern derived from the module's own `path` — only the renderer needs it, because its module path is `src/renderer/` while its root is `src/renderer/src/`. Keys are basenames.",
      "modules": {
        "shared": {
          "allow": {
            "index.ts": "KIND — the module's declared public entry. Every module's root holds exactly this by definition."
          },
          "ratchet": {
            "channels.ts": "RATCHET — the IPC channel-name constants; belongs beside the bridge vocabulary in an `ipc/` (name-for-purpose) folder with the request/result types each channel carries.",
            "cockpitState.ts": "RATCHET — the projected-state shape; belongs in `projection/` with projection.ts and its test, which are the only things that build and read it.",
            "projection.ts": "RATCHET — the pure projection reducer; head of the `projection/` folder it should name.",
            "projection.test.ts": "RATCHET — moves with projection.ts.",
            "projectionSupersede.test.ts": "RATCHET — a second suite over the same reducer (#362); belongs in `projection/` with it.",
            "feedCallRole.ts": "RATCHET — one of eight `feed*` files at this root, the loudest prefix-instead-of-a-folder in the repo. All eight belong in `feed/`.",
            "feedCalls.ts": "RATCHET — belongs in `feed/`.",
            "feedMedia.ts": "RATCHET — belongs in `feed/`.",
            "feedRows.ts": "RATCHET — belongs in `feed/` as its entry; the five feedRows*.test.ts files are its suite.",
            "feedRows.test.ts": "RATCHET — belongs in `feed/`.",
            "feedRowsCalls.test.ts": "RATCHET — belongs in `feed/`.",
            "feedRowsFolding.test.ts": "RATCHET — belongs in `feed/`.",
            "feedRowsMedia.test.ts": "RATCHET — belongs in `feed/`.",
            "feedRowsMutations.test.ts": "RATCHET — belongs in `feed/`.",
            "feedRowsStructural.test.ts": "RATCHET — belongs in `feed/`.",
            "git.ts": "RATCHET — the GitFacts/GitOperation cross-process contract; belongs in `git/`, the mirror of main's own git module.",
            "lifecycleModel.ts": "RATCHET — the session-facts → delivery-render-state derivation; belongs in `delivery/` with its test, the CONTEXT.md L4 vocabulary it speaks.",
            "lifecycleModel.test.ts": "RATCHET — moves with lifecycleModel.ts.",
            "honesty.ts": "RATCHET — the DIRECT/DERIVED/CONVENTION tier; a Session-observation concept, belongs in `session/` with the runtime tree it annotates.",
            "runtimeTree.ts": "RATCHET — the locked Agent · Subagent · Turn · Tool Call tree; head of the `session/` folder it should name.",
            "sessionFacts.ts": "RATCHET — belongs in `session/`.",
            "sessionPosture.ts": "RATCHET — the managed/external/orphaned axis; belongs in `session/`.",
            "projects.ts": "RATCHET — the Project shape and name derivation; belongs in `projects/` with its test, mirroring main's own projects module.",
            "projects.test.ts": "RATCHET — moves with projects.ts."
          }
        },
        "main": {
          "allow": {
            "index.ts": "KIND — the Electron main entry: creates the window and calls each domain's `wire*`. Belongs to no domain by definition."
          },
          "ratchet": {}
        },
        "preload": {
          "allow": {
            "index.ts": "KIND — the module's declared public entry, and the file Electron loads by path.",
            "index.d.ts": "KIND — the ambient shape of the bridge that entry exposes. Its whole job is to sit beside it."
          },
          "ratchet": {}
        },
        "renderer": {
          "path": "^src/renderer/src/[^/]+$",
          "comment": "The composition root: it holds only what belongs to no module — the bootstrap, the container that wires the slices, and its View. Slice-owned files and slice-serving wiring both have homes (`<slice>/` and `cockpit/`), so a file loose here is a file that skipped the decision.",
          "allow": {
            "main.tsx": "KIND — the bootstrap: mounts App into the DOM. Belongs to no module by definition.",
            "env.d.ts": "KIND — ambient renderer types (the preload bridge shape, the csstype augmentation). Not a module's code.",
            "App.tsx": "KIND — the container (rules/ui-components.md): reads the store through `cockpit/` and renders the root View. The one file allowed to know both.",
            "CockpitScreenView.tsx": "KIND — the root View: props in, JSX out, composing shell + active room. Its whole job is to sit outside every slice.",
            "CockpitScreenView.stories.tsx": "KIND — the root View's story, which ui-components.md makes mandatory. Lives beside its View."
          },
          "ratchet": {}
        },
        "renderer-shared": {
          "comment": "Nothing may sit loose here at all: the tier's whole contract is that external code reaches it through the three declared barrels, so a file at its root would be reachable by no legal import.",
          "allow": {},
          "ratchet": {}
        },
        "cockpit": {
          "allow": {
            "index.ts": "KIND — the module's declared public entry, which App.tsx is the only caller of.",
            "RoomStage.tsx": "KIND — the stage that picks the active room's component. The module's comment names it as half of what this module IS, and it composes the rooms from outside all of them, so it belongs to no sub-domain within it."
          },
          "ratchet": {
            "sessionStore.ts": "RATCHET — the Zustand store; belongs in a `projection/` folder holding the store and the delta application that feeds it, since reading the projection is what this module does.",
            "sessionStore.test.ts": "RATCHET — moves with sessionStore.ts.",
            "useGitFacts.ts": "RATCHET — one of six `use*` hooks loose at this root. Group by the surface each serves, not by the fact they are hooks (`hooks/` is banned): this and useGitGroup belong in `git/`.",
            "useGitGroup.ts": "RATCHET — belongs in `git/` with useGitFacts.",
            "useShellCommands.ts": "RATCHET — belongs in a `shell/` wiring folder with useShellKeymap and useShellState, the three that assemble the shell slice's props.",
            "useShellKeymap.ts": "RATCHET — belongs in the `shell/` wiring folder.",
            "useShellState.ts": "RATCHET — belongs in the `shell/` wiring folder.",
            "useSessionInterior.ts": "RATCHET — the Sessions room's wiring; belongs in a `sessions/` folder here, the mirror of the room it serves."
          }
        },
        "shell": {
          "comment": "This module's public entry is `components/index.ts`, so its root holds no entry file — every file here is a model or a test that should be inside a sub-domain.",
          "allow": {},
          "ratchet": {
            "branchMenuModel.ts": "RATCHET — the branch-menu model; belongs in `git/` beside components/git/, whose BranchMenu it is the model for.",
            "branchMenuModel.test.ts": "RATCHET — moves with branchMenuModel.ts.",
            "worstStateDot.ts": "RATCHET — the git group's worst-state roll-up; belongs in `git/`.",
            "worstStateDot.test.ts": "RATCHET — moves with worstStateDot.ts.",
            "projectUi.ts": "RATCHET — the project-strip presentation; belongs in `project-strip/` beside components/project-strip/.",
            "projectUi.test.ts": "RATCHET — moves with projectUi.ts.",
            "shellCommand.ts": "RATCHET — the ⌘K palette's command list; belongs in `palette/` with the keymap it is bound through.",
            "shellCommand.test.ts": "RATCHET — moves with shellCommand.ts.",
            "shellModel.ts": "RATCHET — the top-bar region model; belongs in `top-bar/` beside components/top-bar/.",
            "shellModel.test.ts": "RATCHET — moves with shellModel.ts."
          }
        },
        "rooms-sessions": {
          "comment": "The worst of the four: seven `interior*` files are a folder wearing a prefix as a disguise. Its public entry is `components/index.ts`, so nothing legitimately sits at this root.",
          "allow": {},
          "ratchet": {
            "interiorModel.ts": "RATCHET — head of the `interior/` folder the seven interior* files should be; the prefix is already the folder name.",
            "interiorModel.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorActivity.ts": "RATCHET — belongs in `interior/`.",
            "interiorActivity.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorDock.ts": "RATCHET — belongs in `interior/`.",
            "interiorDock.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorHeader.ts": "RATCHET — belongs in `interior/`.",
            "interiorHeader.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorSubagents.ts": "RATCHET — belongs in `interior/`.",
            "interiorSubagents.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorSubagentRows.test.ts": "RATCHET — belongs in `interior/`.",
            "interiorTimeline.ts": "RATCHET — belongs in `interior/`.",
            "interiorTurnTitle.test.ts": "RATCHET — belongs in `interior/`.",
            "activityStates.ts": "RATCHET — the Activity surface's state vocabulary; belongs in `interior/` with the activity model that reads it.",
            "contextEstimate.ts": "RATCHET — the `~n%` DERIVED context estimate; belongs in a `usage/` folder with its test — a Usage concept, not a room-level one.",
            "contextEstimate.test.ts": "RATCHET — moves with contextEstimate.ts.",
            "sessionClock.ts": "RATCHET — turn/session duration; belongs in `usage/` beside contextEstimate, the other per-session telemetry derivation.",
            "sessionClock.test.ts": "RATCHET — moves with sessionClock.ts.",
            "sessionPlan.ts": "RATCHET — the Session-scoped Plan (ADR-0020); belongs in `plan/` with its test.",
            "sessionPlan.test.ts": "RATCHET — moves with sessionPlan.ts.",
            "sessionsRoomModel.ts": "RATCHET — the room's own model; belongs in `roster/` with the rail it feeds, or is the one file that earns an allow here once the rest have moved and the decision can be made on a root of one.",
            "sessionsRoomModel.test.ts": "RATCHET — moves with sessionsRoomModel.ts.",
            "useSpineLayout.ts": "RATCHET — the feed spine's layout measurement; belongs in `components/` beside the FeedSurface that is its only caller.",
            "useSpineLayout.test.ts": "RATCHET — moves with useSpineLayout.ts."
          }
        },
        "rooms-work": {
          "comment": "Unbuilt (#157 inventory). The entry exists so the module is guarded from its first file rather than after it has flattened — which is the whole point of ADR-0021.",
          "allow": {},
          "ratchet": {}
        },
        "rooms-code": {
          "comment": "Unbuilt (#157 inventory). Guarded from its first file, as above.",
          "allow": {},
          "ratchet": {}
        },
        "delivery": {
          "comment": "RETIRED (#157). Its surviving files are all under components/; nothing sits at its root and nothing new may.",
          "allow": {},
          "ratchet": {}
        }
      }
    },
    "earnedShared": {
      "comment": "A symbol earns the shared tier on its THIRD caller (rules/file-structure.md), but the gate enforces the FLOOR at two: the rule calls two callers 'either — copy it, or hoist', so only a single-caller symbol is unambiguously misplaced and arithmetic must not adjudicate a judgement call. Counted per symbol across modules, because everything leaves the tier through a barrel and a file-level graph would show every module depending on every leaf behind it.",
      "module": "renderer-shared",
      "minImportingModules": 2,
      "exclude": ["^src/renderer/src/shared/components/"],
      "ratchet_": "Each entry below is a symbol in the domain-aware tier with one caller left. These rot SILENTLY rather than reddening: earned-shared-check.mjs defines stale as 'now wanted by >= 2 modules', so when the last caller is deleted the count drops to zero and the gate says nothing. The ticket that deletes the caller deletes the entry.",
      "_exclude_comment": "KIND — the framework-primitive kit (`components/ui`, its icons, the docs blocks). file-structure.md earns the generic tier by CATEGORY, not by caller count: these would stand alone as a published package, so a vendored Radix wrapper or an icon belongs to the kit from its first caller. Counting callers here would flag 50 icons and demand they be scattered into the slices that happen to use one each.",
      "ratchet": {
        "DeliveryState": "KIND — the declared return type of `deliveryState`, whose function this tier already earns. The caller-count rule cannot apply to a function's own return type: it has no home other than beside the function, so a single caller does not make it misplaced and a second caller would not move it. Named here rather than re-derived at the call site as `ReturnType<typeof deliveryState>`, which only hid it from this count.",
        "deliveryState": "RATCHET — the one Session → (lifecycle, rail status) derivation. Its second caller was the renderer root's `sessionScreenModel`, which #268 deleted with the retired interior; read only by `rooms-sessions` until the Delivery review surface becomes a room (#269), which is the ticket that either earns it the tier back or moves it.",
        "DotGlow": "KIND — `StatusDot`'s own `glow` prop type, declared beside the tone vocabulary it belongs to. Its real second caller is the primitive itself, which sits in the `components/` tree this count excludes BY CATEGORY, so the arithmetic can never see it: moving the type into the one room that also names it would put a kit primitive's prop type inside a slice.",
        "RosterIcon": "RATCHET — the status-icon type, read only by `delivery` today; earns the tier again once a second room renders a status icon (#269).",
        "isHotHeadState": "RATCHET — the lifecycle's stalled-on-a-human state set. Its second caller was the rail's pulse budget, which issue 267 retired: a session's dot now pulses on its own state, so nothing asks whether the lifecycle is louder. Read only by `delivery`'s LifecycleNode until the Delivery strip becomes a room (issue 269), which is also when it either earns the tier back or moves into that module."
      }
    },
    "kindFolders": {
      "comment": "Folders named for what files ARE rather than what they're for. `lib/` is absent deliberately: rules/file-structure.md names it as the one sanctioned shared tier. `components/` is absent because a slice's `components/` holds one feature's components, which is the opposite of a junk drawer.",
      "banned": [
        "schemas",
        "types",
        "utils",
        "helpers",
        "constants",
        "interfaces",
        "validators",
        "handlers",
        "hooks",
        "models"
      ],
      "ratchet": {
        "src/renderer/src/hooks": "RATCHET — pre-#264 kind-folder holding a SECOND, API-divergent `useDisclosure` ({isOpen,open,close,toggle}) beside the kit's kept tuple version. Its only importers are `delivery`'s FileDiff and CommitGroup, so the folder, this entry and the `components.json` \"hooks\" alias all die together when #271 lands — one commit, since kind-folder-check.mjs fails on a stale key as well as a breach."
      }
    }
  }
}
