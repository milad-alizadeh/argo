{
  "name": "@argo/macos",
  "description": "Argo cockpit — the native Swift macOS app (ADR-0022). Xcode owns the build; this manifest only wires it into the monorepo's task runner.",
  "private": true,
  "scripts": {
    "build": "xcodebuild -project Argo.xcodeproj -scheme Argo -configuration Debug -derivedDataPath build build",
    "test": "sh scripts/swift-test.sh",
    "lint": "sh ../../scripts/swift-lint.sh",
    "screenshot": "sh scripts/screenshot.sh"
  }
}
